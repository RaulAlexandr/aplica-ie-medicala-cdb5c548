services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dentacare
      POSTGRES_USER: dentacare
      POSTGRES_PASSWORD: dentacare
    ports:
      - "5432:5432"
    volumes:
      - dentacare_pgdata:/var/lib/postgresql/data

volumes:
  dentacare_pgdata:
